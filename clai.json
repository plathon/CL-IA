{
    "overview": "You are coding a todo app with nextjs and tailwind the backend part of project should be mocked inside frontend",
    "explain": [
        {
            "folder": "./src/bin",
            "explanation": "contains the source code of the commands"
        }
    ],
    "commands": [
        {
            "name": "example",
            "args": ["name"],
            "templates": [{
                "template": "{{name}}-controller.ts",
                "outFile": "./src/controllers/{{name}}-controller.ts"
            },
            {
                "template": "{{name}}-view.ts",
                "outFile": "./src/views/{{name}}-view.ts"
            }],
            "instructions": ["you should import the state lib from react package inside every page"]
        }
    ],
    "extensions": [".ts"],
    "includes": ["./src/bin", "./src/schemas"]
}